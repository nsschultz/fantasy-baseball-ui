import React from "react";

/**
 * Creates a new image using a version of the logo image. The image itself is not currently modifiable.
 * @param {*} props (Optional) Any additional properties for the logo.
 * @returns A new instance of the Logo.
 */
const Logo = (props) => <img alt="logo" src="/static/logo-056.png" {...props} />;
export default Logo;
