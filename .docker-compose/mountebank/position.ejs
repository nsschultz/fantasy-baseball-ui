{
  "port": 5555, "protocol": "http", "name": "positions", "defaultResponse": { "statusCode": 400, "body": "Bad Request", "headers": {} },
  "stubs": [
    {
      "predicates": [{ "equals": { "method": "GET", "path": "/api/health" } }],
      "responses": [{ "is": { "statusCode": 200, "body": "Healthy" } }]
    },
    {
      "predicates": [{ "equals": { "method": "GET", "path": "/api/v1/position" } }],
      "responses": [{ 
        "is": { 
          "statusCode": 200, 
          "body": "<%- stringify(filename, './responses/positions.json') %>",
          "headers": { 
            "Access-Control-Allow-Origin": "http://localhost:3000",
            "Content-Type": "application/json; charset=utf-8"
          }
        } 
      }]
    }
  ]
}